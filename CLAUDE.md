# مشروع: اختراق حاجز الإنجليزية

## السياق
تطبيق ويب تعليمي مصمم لشخص عربي عمره فوق ٤٠ سنة، يملك مفردات إنجليزية قوية لكن يعاني من:
- فهم الكلام المسموع
- تركيب الجمل في الاجتماعات
- الحرج عند التحدث أمام أجانب
- ١٧ سنة محاولات بالطرق التقليدية بدون نتيجة

## المنهجية
برنامج ١٢ أسبوع — ٣٥ دقيقة يومياً — ٥ أنشطة:
1. **تقنية الظل (Shadowing)** - تلقين بصري، جمل تظهر مع عدّاد، المستخدم يقرأ بصوت عالٍ
2. **تفكير بصوت عالٍ** - موضوع يومي + جمل بداية (sentence starters) للتحدث
3. **قراءة قصة** - قصص إنجليزية بنظام التلقين البصري
4. **مراجعة جمل جاهزة** - ٢٥ جملة للاجتماعات مع عداد تكرار
5. **قصة اليوم** - قصة إضافية مختلفة

## القيود التقنية الحرجة
- **ممنوع YouTube embeds** — محظور داخل artifacts
- **ممنوع Spotify embeds** — محظور
- **ممنوع SpeechSynthesis API** — لا يعمل داخل artifacts
- **ممنوع أي iframe** — كلها محظورة
- **ممنوع localStorage/sessionStorage** — لا يعمل في artifacts
- **التخزين**: استخدم `window.storage.get/set` فقط (Anthropic persistent storage API)
- التطبيق يجب أن يكون **مكتفي ذاتياً بالكامل** — صفر مواقع خارجية

## البنية الحالية
- `src/App.jsx` — التطبيق الكامل (React component واحد + sub-components)
- ٤ تبويبات: اليوم، تدريب، الجمل، التقدم
- تصميم داكن RTL عربي
- خط IBM Plex Mono للإنجليزي، Noto Kufi Arabic للعربي

## المطلوب تنفيذه (الفجوات)

### إصلاحات حرجة
1. **خلط الخيارات عشوائياً** — في محاكاة الاجتماع والاستجابة السريعة والاختبار الأسبوعي. حالياً الإجابة الصحيحة دائماً الخيار الأول (`ans: 0`). يجب إنشاء دالة `shuffleOpts(correct, wrong, seed)` تخلط الخيارات وتعيد `{opts, correctIndex}`.

2. **جمل البداية (Sentence Starters)** — في نشاط "تفكير بصوت عالٍ". حالياً يعطي موضوع فقط. المطلوب: إضافة ٤-٥ جمل بداية لكل موضوع مثل: "First, I usually...", "Then I...", "After that..." — تظهر تحت الموضوع بلون مميز.

3. **حفظ نتائج الاختبار الأسبوعي** — حالياً النتائج تضيع. المطلوب: حفظها في `window.storage` ومقارنتها بالاختبار السابق (📈 +20%) + رسم بياني بسيط في تبويب التقدم.

### تمارين جديدة (تبويب التدريب)
4. **📝 أكمل الفراغ** — جملة من بنك الجمل مع فراغات. المستخدم يكتب الكلمات الناقصة. يختبر الحفظ الحقيقي.

5. **🧩 بناء جمل** — كلمات مبعثرة، المستخدم يضغط عليها بالترتيب الصحيح. يعالج مشكلة تركيب الجمل.

### تحسينات المحتوى
6. **قصص أكثر** — حالياً ٣ فقط، تتكرر بسرعة. أضف ٣-٥ قصص إضافية بنفس النمط (١٠ جمل، موضوعات عمل).
7. **سيناريوهات اجتماعات أكثر** — حالياً ٢ فقط. أضف ٢-٣ إضافية.
8. **جمل Shadowing متدرجة** — مستوى ١ (جمل قصيرة) للمرحلة ١، مستوى ٢ (جمل متوسطة) للمرحلة ٢، مستوى ٣ (جمل طويلة) للمرحلة ٣.

## أوامر التشغيل
```bash
npm install
npm run dev
```

## ملاحظات للتطوير
- الملف الحالي يعمل — لا تعيد بناءه من الصفر
- أضف التعديلات واحدة واحدة واختبر بعد كل تعديل
- اختبر على الجوال (RTL + شاشة صغيرة)
- الـ persistent storage API: `await window.storage.get(key)` / `await window.storage.set(key, value)`
- لا تستخدم أي مكتبة خارجية غير React
